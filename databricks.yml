bundle:
  name: hhs-lakehouse-architecture

sync:
  include:
    - .build

artifacts:
  default:
    build: apx build

resources:
  apps:
    hhs-lakehouse-architecture-app:
      name: "hhs-lakehouse-architecture"
      description: "hhs-lakehouse-architecture created with apx"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true
